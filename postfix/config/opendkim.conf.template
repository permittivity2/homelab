# Managed by homelab-postfix -- do not hand-edit, edit debconf answers
# instead (dpkg-reconfigure homelab-postfix). Replaces the vendor's own
# /etc/opendkim.conf wholesale (OpenDKIM has no drop-in include
# directory the way Postfix/PowerDNS do) -- only written at all when
# the homelab-postfix/enable_dkim debconf flag is true.
#
# KeyTable/SigningTable are regenerated by homelab-domain-admin itself
# (see ../../domain-admin/README.md's "DKIM" section) every time a
# selector is rotated/activated/retired -- this package only creates
# them empty (with the right ownership) and points OpenDKIM at them.
# Only 'active' selectors ever appear in these two files: OpenDKIM only
# consults them when SIGNING outbound mail, never when a remote server
# verifies a signature (that's a pure DNS TXT lookup) -- so a 'pending'
# or 'retiring' selector correctly has no KeyTable/SigningTable entry
# at all, even though its key file and TXT record both still exist.
# OpenDKIM refuses to sign with a key if its own "safe keys" check
# considers the key file's group insecure -- which trips UNCONDITIONALLY
# under this design, on every install, not just as some edge case: the
# opendkim daemon account's own primary group already IS `opendkim`,
# and `homelab` (homelab-domain-admin's service account, which needs
# write access to generate/rotate keys) is a second, deliberately-added
# member of that same group -- two members is already "multiple users"
# to this check, regardless of anyone else. A real `homelab-cli dns
# dkim rotate` -> real send caught this outright ("key data is not
# secure: root is in group ... which has multiple users") -- disabling
# the check is the correct call here, not a weakening of an otherwise-
# avoidable risk: the actual security boundary this design relies on is
# "only opendkim and homelab can read /etc/opendkim/keys at all" (mode
# 640, group opendkim, no other members ever added -- see README.md),
# which RequireSafeKeys has no way to distinguish from a genuinely
# loosely-shared key directory.
RequireSafeKeys no
Syslog          yes
UMask           007
Mode            sv
Domain          *
KeyTable        /etc/opendkim/KeyTable
SigningTable    /etc/opendkim/SigningTable
ExternalIgnoreList  refile:/etc/opendkim/TrustedHosts
InternalHosts       refile:/etc/opendkim/TrustedHosts
Socket          inet:8891@localhost
PidFile         /run/opendkim/opendkim.pid
