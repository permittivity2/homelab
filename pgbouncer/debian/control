Source: homelab-pgbouncer
Section: database
Priority: optional
Maintainer: Jeff Gardner <mailmasker-api01@forge.name>
Build-Depends: debhelper-compat (= 13)
Standards-Version: 4.6.2
Homepage: https://github.com/permittivity2/homelab

Package: homelab-pgbouncer
Architecture: all
Depends: ${misc:Depends}, pgbouncer, homelab-common
Recommends: homelab-database, homelab-agent
Description: PgBouncer connection pooler in front of the shared homelab database
 Configures PgBouncer (scram-sha-256 auth, a static userlist.txt) with a
 `homelab` database entry pointing at a homelab-database-managed Postgres
 backend, local or remote. Config is a single drop-in file activated via
 pgbouncer's own native %include directive -- never hand-edits
 pgbouncer.ini beyond that one line. The tool other homelab-* packages'
 installers call to register a feature's RUNTIME role's SCRAM secret
 into userlist.txt (homelab-bootstrap-pgbouncer-entry) lives in
 homelab-common instead of here, so any feature host can depend on the
 lightweight client tool without this whole package (and a full local
 pgbouncer server) coming along with it. This package itself needs a
 hard Depends (not just Recommends) on homelab-common too: a remote
 host's bootstrap SSHes INTO the pgbouncer host and runs that exact
 tool locally there to register itself -- found missing on a fresh
 install where nothing had ever pulled homelab-common onto the
 pgbouncer host itself, leaving every remote registration attempt with
 no binary to SSH into.
