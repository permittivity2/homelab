#!/bin/sh
set -e

. /usr/share/debconf/confmodule

# Already configured (fresh reinstall/upgrade) — don't re-prompt.
if [ -f /etc/homelab/api/config.yml ]; then
    exit 0
fi

db_input high homelab-api/db_host || true
db_input high homelab-api/pgbouncer_host || true
db_go

exit 0
